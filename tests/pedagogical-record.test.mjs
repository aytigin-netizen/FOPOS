import assert from "node:assert/strict";import test from "node:test";import{approveRecord,createPedagogicalRecord,deriveProduct,reviseRecord,submitForReview}from"../app/core/pedagogical-record.ts";
const unit={code:"F10_U1",name:"Felsefenin Doğası",hours:10,grade:10,keywords:[],outcomes:[{code:"FEL.10.1.1",description:"x",short:"x"}],strategy:"Sorgulama",methods:[],opening:"",inquiry:"",discussion:"",application:"",evidence:"Kanıt"};
test("kayıt taslak başlar ve yalnız inceleme üzerinden onaylanır",()=>{const draft=createPedagogicalRecord({unit,outcomeCode:"FEL.10.1.1",week:1,profile:"Dengeli",datasetVersion:"2024.1"});assert.equal(draft.status,"draft");assert.throws(()=>approveRecord(draft,"onay"));const approved=approveRecord(submitForReview(draft),"Kontrol ettim");assert.equal(approved.status,"approved");assert.equal(approved.approval.actorRole,"teacher")});
test("değişiklik yeni ve onaysız revizyon oluşturur",()=>{const first=createPedagogicalRecord({unit,outcomeCode:"FEL.10.1.1",week:1,profile:"Dengeli",datasetVersion:"2024.1"});const{previous,next}=reviseRecord(first,{lessonContext:{...first.lessonContext,week:2}});assert.equal(previous.status,"superseded");assert.equal(next.revision,2);assert.equal(next.status,"draft");assert.equal(next.approval,null)});
test("ürün kaynak kayıt ve revizyona bağlıdır",()=>{const record=createPedagogicalRecord({unit,outcomeCode:"FEL.10.1.1",week:1,profile:"Dengeli",datasetVersion:"2024.1"});const product=deriveProduct(record,"lesson_design");assert.equal(product.sourceRecordId,record.recordId);assert.equal(product.sourceRevision,record.revision)});
